export default function NewService() {
  // take in  the current asset id,
  // add a new service to the asset with specific data from form
  // custom inputs to handle buttons?
  // have a function that checks the when the last service date was compared to today,
  // ++ red for action required, yellow for monitor, and green normal

  return <div>NewService</div>;
}
